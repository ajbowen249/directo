# directo

`directo` is a directive-processing framework for text-based storytelling RPGs.

## Building and Testing

`directo` uses `yarn`.

```bash
yarn
```

To run the tests:

```bash
yarn test
```
